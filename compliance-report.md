# Suki Engine — Compliance Report

Generated: 2026-06-07
Engine: @kap-solo/suki-engine v0.1.0
Reference game: pure-plinko
Math bundle: ../Pure-Plinko/data

Self-assessment for Stake-shaped game development. Automated checks run locally against the mock RGS; live Stake proof requires provider access.

## Test harness

| Suite | Result | Detail |
|-------|--------|--------|
| Smoke tests | PASS | 69 passed |
| ERR scenarios | PASS | 89 passed |
| Stake RGS sandbox | SKIP | set SUKI_RGS_URL, SUKI_SESSION_ID, SUKI_GAME_ID |
| Math validation | PASS | — |

## Readiness snapshot (self-assessment)

| Layer | Estimate | Basis |
|-------|----------|-------|
| Engine foundation | ~92% | Wallet lifecycle, book player, bootstrap APIs, 69 smoke tests |
| Jurisdiction & UI policy | ~88% | Control policy, session timer, social copy, i18n scaffold |
| Mock RGS / local proof | ~90% | 89 ERR scenarios, mock injection harness |
| Production / live RGS | ~25% | Sandbox not run — needs SUKI_RGS_URL + session |
| Math bundle | ~70% | validate-math passed on bundle |
| **Ship-ready (overall)** | **~68%** | All run checks passed |

## RGS wallet contract

- [x] POST /wallet/authenticate — smoke + mock RGS
- [x] POST /wallet/balance — smoke + mock RGS
- [x] POST /wallet/play — smoke + mock RGS
- [x] POST /wallet/end-round — smoke + mock RGS
- [x] POST /bet/event (per book step) — smoke + book player
- [x] POST /bet/action (in-round picks) — smoke + mock RGS
- [x] GET /bet/replay/{game}/{version}/{mode}/{event} — mock RGS + replay mode
- [ ] POST /game/search — optional Stake endpoint

## Round lifecycle

- [x] Outcome from RGS before client animation
- [x] Generic book event player (handler map)
- [x] Resume active round from last bet/event
- [x] Replay mode (read-only, no wallet writes)
- [x] Minimum round duration (jurisdiction)
- [x] Bet modes / cost multipliers (BASE, buy, activate)

## Jurisdiction & player display

- [x] Turbo / super-turbo / autoplay / slamstop / spacebar gating
- [x] Buy feature gating (disabledBuyFeature)
- [x] Session timer (displaySessionTimer)
- [x] Social casino copy (balance → coins, etc.)
- [x] Currency from auth + URL through play()
- [x] i18n scaffold (lang param)

## Engine APIs (game integration)

- createGameBootstrap()
- createSukiLifecycle() + book handlers
- createBetModePolicy()
- parseAuthResponse()
- createControlPolicy()
- createSessionTimer()
- createCurrencyFormatter() / game.formatCurrency()
- createCopyPolicy() / game.t()
- classifyRgsError() + withRgsCall() retry
- createMockRgs() prototype server

## Requires Stake provider access

- Live Stake RGS session (real sessionID + wallet debit/credit)
- Math publish through Stake ACP / certification pipeline
- Production CDN deploy via Stake provider workflow
- Formal jurisdiction certification per market

## Suggested next steps

- Set `SUKI_RGS_URL`, `SUKI_SESSION_ID`, `SUKI_GAME_ID` and re-run `npm run check` for live sandbox proof.
- Attach this report + game outline + demo URL to Stake provider application.
- Keep `npm run check` in CI on every engine push.

---

*Report generated by Suki Engine compliance checklist. Re-run: `npm run check -- --math ../Pure-Plinko/data`*